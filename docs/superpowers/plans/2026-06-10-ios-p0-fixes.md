# iOS P0 Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复两个阻塞 iOS 端正常运行的 P0 问题：删除死代码 FrameModifier、并为 MCP 补齐 iOS iproxy 自动连接支持。

**Architecture:**
- P0-1：`FrameModifier.swift` 是从未被调用的死代码（实际修改逻辑在 `ViewModifyService.swift`，已正确使用 `CGAffineTransform`），直接删除即可。
- P0-3：在 `mcp/src/sdk-client.ts` 的 `ensureAdbForward` 函数中新增 iOS 分支，通过 `nc -z` 检测端口、`which iproxy` 验证安装、`spawn` 后台启动 iproxy；注册进程退出钩子清理子进程。

**Tech Stack:** Swift/iOS（UIKit），TypeScript/Node.js（MCP），iproxy（libimobiledevice）

---

### Task 1: 删除死代码 FrameModifier.swift

**Files:**
- Delete: `clients/ios/sdk/Sources/ViewModify/FrameModifier.swift`

> **背景**：`ViewModifyService.swift` 已正确实现 `CGAffineTransform` 方案（applyMove + applySize），`FrameModifier.swift` 从未被调用，是有约束逻辑缺陷的死代码。

- [ ] **Step 1: 确认 FrameModifier 无调用方**

```bash
cd /path/to/client-tools
grep -r "FrameModifier\|modifyFrame\|setFixedDimension" clients/ios/sdk/Sources/
```

期望输出：只有 `FrameModifier.swift` 自身，无其他文件引用。

- [ ] **Step 2: 删除文件**

```bash
rm clients/ios/sdk/Sources/ViewModify/FrameModifier.swift
```

- [ ] **Step 3: 验证 iOS SDK 编译通过**

```bash
cd clients/ios/demo
pod install 2>/dev/null || true
xcodebuild -workspace ClientToolsDemo.xcworkspace \
           -scheme ClientToolsDemo \
           -destination 'generic/platform=iOS Simulator' \
           build 2>&1 | tail -5
```

期望输出：`** BUILD SUCCEEDED **`

- [ ] **Step 4: Commit**

```bash
git add -u clients/ios/sdk/Sources/ViewModify/FrameModifier.swift
git commit -m "refactor(ios): remove dead FrameModifier (ViewModifyService already uses CGAffineTransform)"
```

---

### Task 2: MCP 补齐 iOS iproxy 自动连接支持

**Files:**
- Modify: `mcp/src/sdk-client.ts`

> **背景**：当前 `ensureAdbForward` 只处理 Android（adb forward），iOS 分支为空，USB 连接的 iOS 设备无法建立端口转发。需要补齐 `ensureIosProxy` 并注册进程退出钩子。

- [ ] **Step 1: 更新 import，增加 spawn 和 ChildProcess**

在 `mcp/src/sdk-client.ts` 第 1 行，将：

```typescript
import { execSync } from "child_process";
```

替换为：

```typescript
import { execSync, spawn } from "child_process";
import type { ChildProcess } from "child_process";
```

- [ ] **Step 2: 在 PORTS 常量之后添加 iproxy 状态变量和辅助函数**

在第 14 行 `const PORTS` 定义之后，插入以下代码（放在 `const DEFAULT_TIMEOUT_MS` 之前）：

```typescript
let iosProxyProcess: ChildProcess | null = null;

function isPortListening(port: number): boolean {
  try {
    execSync(`nc -z 127.0.0.1 ${port}`, { stdio: "ignore" });
    return true;
  } catch {
    return false;
  }
}

function ensureIosProxy(): void {
  if (isPortListening(PORTS.ios)) return;
  try {
    execSync("which iproxy", { stdio: "ignore" });
  } catch {
    throw new Error(
      "iproxy not found. Please install libimobiledevice: brew install libimobiledevice"
    );
  }
  const proc = spawn("iproxy", [String(PORTS.ios), String(PORTS.ios)], {
    stdio: "ignore",
    detached: true,
  });
  proc.unref();
  iosProxyProcess = proc;
}

function cleanupIosProxy(): void {
  if (iosProxyProcess) {
    try { iosProxyProcess.kill(); } catch { /* ignore */ }
    iosProxyProcess = null;
  }
}

process.on("exit", cleanupIosProxy);
process.on("SIGINT", () => { cleanupIosProxy(); process.exit(0); });
process.on("SIGTERM", () => { cleanupIosProxy(); process.exit(0); });
```

- [ ] **Step 3: 在 ensureAdbForward 中增加 iOS 分支**

将现有的 `ensureAdbForward` 函数（第 23-31 行）：

```typescript
function ensureAdbForward(platform: Platform): void {
  if (platform === "android") {
    try {
      execSync(`adb forward tcp:${PORTS.android} tcp:${PORTS.android}`, { stdio: "ignore" });
    } catch {
      // adb not available or no device, ignore
    }
  }
}
```

替换为：

```typescript
function ensureAdbForward(platform: Platform): void {
  if (platform === "android") {
    try {
      execSync(`adb forward tcp:${PORTS.android} tcp:${PORTS.android}`, { stdio: "ignore" });
    } catch {
      // adb not available or no device, ignore
    }
  } else if (platform === "ios") {
    ensureIosProxy();
  }
}
```

- [ ] **Step 4: 验证 TypeScript 编译通过**

```bash
cd mcp
npm run build 2>&1 | tail -10
```

期望输出：无报错，`dist/` 目录更新。

- [ ] **Step 5: 手动验证行为（需要 iOS 设备连接）**

```bash
# 确保 iproxy 未运行
pkill iproxy 2>/dev/null || true

# 运行 MCP，触发一次 iOS 请求（需要 iOS 设备通过 USB 连接）
# 检查 iproxy 是否被自动启动
ps aux | grep iproxy | grep -v grep
```

期望：看到 `iproxy 8080 8080` 进程。

- [ ] **Step 6: 验证 iproxy 未安装时的错误提示**

```bash
# 临时重命名 iproxy 模拟未安装
sudo mv $(which iproxy) /tmp/iproxy_bak 2>/dev/null || echo "skip if iproxy not installed"

# 运行后应在 iOS 请求时抛出明确错误
# 恢复
sudo mv /tmp/iproxy_bak $(which iproxy) 2>/dev/null || true
```

期望错误信息：`iproxy not found. Please install libimobiledevice: brew install libimobiledevice`

- [ ] **Step 7: Commit**

```bash
git add mcp/src/sdk-client.ts
git commit -m "feat(mcp): add iOS iproxy auto-forward support with cleanup on exit"
```
